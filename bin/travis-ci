#!/bin/bash

echo ===;
whoami;
echo ===;

YOGI=`readlink -f $(which yogi)`;
YOGI=${YOGI%/*};
cp -f ./bin/grover_patch_wrapper.js $YOGI/../node_modules/grover/lib/wrapper/wrapper.js;

echo patched grover;
echo ===;

for d in src/*; do
	if [[ ! -d $d/tests ]]; then continue; fi

	pushd $d
	if ! yogi build --no-lint; then exit 1; fi
	if ! yogi test --debug; then exit 1; fi
	popd
done
