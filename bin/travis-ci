#!/bin/bash

echo ===;
whoami;
echo ===;

for d in src/*; do
	if [[ ! -d $d/tests ]]; then continue; fi

	pushd $d
	if ! yogi build --no-lint; then exit 1; fi
	if ! yogi test; then exit 1; fi
	popd
done
